name: Evaluate Legacy Terminology

on:
  pull_request:
    paths:
      - 'src/**'
      - 'docs/**'
      - 'samples/**'
      - 'examples/**'
      - 'README.md'
      - 'tests/check_evaluate_legacy_terminology.py'
      - '.github/workflows/evaluate-legacy-terminology.yml'
      - '!src/XPScript.Compiler/Notes*.cs'
      - '!docs/notes-*.md'
      - '!samples/notes-*.xps'
      - '!samples/xpscript-notes-runtime-fixture.dxl'
      - '!todo/HCLNotes.md'
  push:
    branches: [ main ]
    paths:
      - 'src/**'
      - 'docs/**'
      - 'samples/**'
      - 'examples/**'
      - 'README.md'
      - 'tests/check_evaluate_legacy_terminology.py'
      - '.github/workflows/evaluate-legacy-terminology.yml'
      - '!src/XPScript.Compiler/Notes*.cs'
      - '!docs/notes-*.md'
      - '!samples/notes-*.xps'
      - '!samples/xpscript-notes-runtime-fixture.dxl'
      - '!todo/HCLNotes.md'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Reject legacy Evaluate terminology and evaluator references
        run: python3 ./tests/check_evaluate_legacy_terminology.py
