name: Apply publish executable detection fix

on:
  push:
    branches:
      - publish-layout-rework

permissions:
  contents: write

jobs:
  apply:
    if: ${{ !contains(github.event.head_commit.message, '[publish-executable-detection-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply executable detection fix
        run: python3 scripts/apply-publish-executable-detection.py
      - name: Remove one-time migration files
        run: |
          rm scripts/apply-publish-executable-detection.py
          rm .github/workflows/apply-publish-executable-detection.yml
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --exit-code && exit 0
          git commit -m "Fix multi-file executable detection [publish-executable-detection-applied]"
          git push origin HEAD:publish-layout-rework
