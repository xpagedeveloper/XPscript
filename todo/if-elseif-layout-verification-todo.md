# If / ElseIf / Else layout verification TODO

(c) xpagedeveloper.com 2026

- [x] single-line `If ... Then ... ElseIf ... Then ... Else ... End If` regression cases added for If, ElseIf and Else branch selection
- [x] standard multi-line `If / ElseIf / Else / End If` regression added
- [x] split `Then` layout regression retained
- [x] mixed inline/multi-line `ElseIf` layout regression added
- [ ] verify the permanent `Control Flow and Error Handling Compatibility` workflow is green on the exact final PR head
- [ ] archive this TODO to `todo/done/` after exact-head verification and merge
