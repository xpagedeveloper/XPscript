Sub IncludedErlWorker()
    On Error GoTo Handler
    Dim value As Integer
    value = 1
    value = CInt("not-a-number")
    Exit Sub
Handler:
    If Erl <> 5 Then
        Error 9002, "Expected include-local Erl 5 but got " & CStr(Erl)
    End If
    Print "INCLUDE-ERL=" & CStr(Erl)
End Sub
