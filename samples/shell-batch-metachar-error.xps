Sub Main()
    ' Expected runtime error: .cmd/.bat arguments may not contain cmd.exe metacharacters.
    ' Validation happens before cmd.exe is started, so the script does not need to exist.
    Call Shell("not-used.cmd safe&whoami")
End Sub
