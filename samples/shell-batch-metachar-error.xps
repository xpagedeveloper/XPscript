Sub Main()
    ' Expected runtime error: .cmd/.bat arguments may not contain cmd.exe metacharacters.
    ' The local script exists so path validation succeeds, but unsafe arguments must be rejected
    ' before cmd.exe starts it. The script would create shell-batch-unsafe-ran.txt if executed.
    Call Shell(".\samples\shell-batch-sentinel.cmd safe&whoami")
End Sub
