Option Declare

Sub Main()
    Dim q As String
    Dim command As String

    q = Chr(34)
    command = q & Application.Args(0) & q
    command = command & " " & q & Application.Args(1) & q
    command = command & " " & q & "space value" & q
    command = command & " " & q & q
    command = command & " " & q & "åäö 漢字" & q
    command = command & " " & q & "semi;dollar$bracket[]" & q

    Print "SHELL-COMMAND=" & command
    Call Shell(command)
    Print "SHELL-ARGUMENTS-LAUNCHED"
End Sub
